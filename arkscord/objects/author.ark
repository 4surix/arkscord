# ArkScript 4.5.0
# ----------------------------------------------------------------------------

(let create-object-author (fun (id name avatar discriminator publicFlags bot?) {
  (fun (
    # Attributs #
    &id
    &name
    &avatar
    &discriminator
    &publicFlags
    &bot?) ()) }))
